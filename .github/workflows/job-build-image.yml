name: Build Image

on:
  workflow_call:

jobs:
  build:
    runs-on: [self-hosted, local, ticket]

    steps:
      - name: Code checkout
        uses: actions/checkout@v5

      - name: Setup JDK
        uses: ./.github/actions/setup-jdk

      - name: Build and Push Native Image to Local Registry
        run: |
          REGISTRY="kind-registry:5000"
          docker build -f Dockerfile.native -t $REGISTRY/api-ticket:${{ github.sha }} .
          docker push $REGISTRY/api-ticket:${{ github.sha }}
